@page "/counter"

<PageTitle>Counter</PageTitle>

<h1>Counter Page</h1>

<p role="status">Current count: @currentCount</p>

<button class="btn btn-primary" @onclick="IncrementCount">Click me see the change</button>

@code {
    private int currentCount = 0;

    private void IncrementCount()
    {
        currentCount+=2;
        Console.WriteLine($"[Now is yes]: {DateTime.Now.ToString("yyyyMMddHHmmssfff")}");
    }
}
